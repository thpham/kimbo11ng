.omc/
target/
**/target/

# EJBCA dependency JARs (extracted from base image via `just extract-jars`)
deps/
deps/**

# reference sources (local only)
.ref/
.ref/**

# Build artifacts
*.class
*.jar

# Local compose overrides, e.g. host port mappings (see docker-compose.yml)
.env
